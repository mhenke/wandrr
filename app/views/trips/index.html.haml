%h1 Listing trips
= link_to 'New Trip', new_trip_path, class: 'btn btn-link'
%table.table.table-striped
  %thead
    %tr
      %th Name
      %th Description
      %th
  - @trips.each do |trip|
    %tr
      %td= link_to trip.name, trip
      %td= trip.description
      - if user_signed_in? && current_user == trip.user
        %td
          =link_to glyph(:edit), edit_trip_path(trip), class:'btn'
          = link_to raw('<i class="icon-remove icon-white"></i> Destroy'), trip, method: :delete, data: { confirm: 'Are you sure?' }, class:'btn'